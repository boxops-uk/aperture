# Moved

This document was retired at pre-release. Its content lives in **website/content/invariants.md — the canonical registry**.

The full text is in git history: `git log --follow -- docs/invariants.md`.
