# Moved

This document was retired at pre-release. Its content lives in **history — the schema DSL is built; the design is website/content/schema-language.md**.

The full text is in git history: `git log --follow -- docs/phase-8-schemas.md`.
