# Moved

This document was retired at pre-release. Its content lives in **history — the viewer is built; see website/content/clients.md**.

The full text is in git history: `git log --follow -- docs/phase-11-code-search.md`.
