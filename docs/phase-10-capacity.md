# Moved

This document was retired at pre-release. Its content lives in **history — the method is website/content/performance.md; the eight hypotheses are bench/FINDINGS.md's appendix**.

The full text is in git history: `git log --follow -- docs/phase-10-capacity.md`.
