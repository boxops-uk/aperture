# Moved

This document was retired at pre-release. Its content lives in **website/content/storage.md — the design book is the website now**.

The full text is in git history: `git log --follow -- docs/02-tuple-codec.md`.
