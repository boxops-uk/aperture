# Moved

This document was retired at pre-release. Its content lives in **website/content/schema-language.md — the design book is the website now**.

The full text is in git history: `git log --follow -- docs/06-types-and-schema.md`.
