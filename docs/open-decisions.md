# Moved

This document was retired at pre-release. Its content lives in **PLAN.md's settled-decisions record; the one open decision is in its stored-derivation section**.

The full text is in git history: `git log --follow -- docs/open-decisions.md`.
