# Moved

This document was retired at pre-release. Its content lives in **history — unions are built; the decisions live in the registry (I10), website/content/storage.md and PLAN.md's settled record**.

The full text is in git history: `git log --follow -- docs/phase-8.6-unions.md`.
