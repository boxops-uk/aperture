# Moved

This document was retired at pre-release. Its content lives in **PLAN.md's authentication section — condensed, nothing built**.

The full text is in git history: `git log --follow -- docs/auth.md`.
