# Moved

This document was retired at pre-release. Its content lives in **superseded: what it proposed is either built (see website/content/query-language.md) or in PLAN.md's language backlog**.

The full text is in git history: `git log --follow -- docs/query-surface.md`.
